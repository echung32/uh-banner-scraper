# @astrojs/cloudflare's prism vite plugin requires prismjs but doesn't declare
# it, which Yarn PnP rejects as an unsound require. Declare it here so PnP can
# resolve it.
packageExtensions:
  "@astrojs/cloudflare@*":
    dependencies:
      prismjs: "^1.30.0"
